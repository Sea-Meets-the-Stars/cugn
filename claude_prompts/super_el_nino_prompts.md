# Studying the El Nino of 2026-2027 on Line 66

This prompt doc will examine the El Nino of 2026-2027 on Line 66.

## Context 

See the `cugn-climatology` repository located under `Oceanography/python`.
There are two files in `context/` that you have generated:

- `context/context_cugn_2026beta.md`
- `context/initial_exploration.md`

See the CUGN webpages at https://spraydata.ucsd.edu/`
In particular:

- `https://spraydata.ucsd.edu/products/el-nino/`
- `https://spraydata.ucsd.edu/products/cugn-climatology/`

Glider data from CUGN is actively placed at this website: `https://ioos.github.io/glider-dac/`

## Code

Follow these guidelines:

- Use inline comments to explain the code.
- Use matplotlib
- Code in Python
- Place imports at the top of modules
- Reuse any code in this repository to help you
- Use functions and not classes
- Include a description of inputs/outputs in the doc string of all methods 
- Add "Generated by JXP and Claude" in the doc string of all methods you create

If you need to run Python code, please use the `ocean14` conda environment.

## Prompts

### Report

1. We are going to generate a report that describes the current state of the El Nino of 2026-2027 on Line 66.  
As the months go by, we will update it.  Begin by:

    - Examining all of the files and websites in context
    - Searching the web for stories and papers on the El Nino of 2026-2027
    - Ask me a set of questions in Q&A/Report to guide your efforts.  If you need me to 

Do not write the report yet.  Use Fable if you can. Log your work.

2. I have answered your questions in Q&A/Report.  Please read my responses and ask another round of questions before doing anything, e.g. discuss where to download the ancillary data onto this machine.  Do not write the report yet.  Use Fable if you can. Log your work.

3. I have answered your questions in Q&A/Report.  Please read my responses and ask another round of questions before doing anything.  I expect this will be the last round before proceeding with the report. Use Fable if you can. Log your work.

4. Ok, I have answered the last round of questions.  Please proceed with the report.  Use Fable if you can. Log your work.

## Q&A

### Report

**What I found before asking (2026-09-03).** Short digest so the questions make
sense; the report itself is not started.

- *Equatorial state.* NOAA CPC (13 Aug 2026): El Niño Advisory; July Niño-3.4
  +1.4 °C, Niño-1+2 +2.9 °C, subsurface anomalies to +10 °C with a deep
  thermocline; >90 % chance of a **very strong** event in fall/winter 2026-27 and
  a 69 % chance OND exceeds every event since 1950 (ONI ≥ +2.5). IRI (19 Aug):
  25 of 26 models ≥ +2.0 °C through DJF, peak OND, some ≥ +3.0 °C; El Niño
  probability 100 % through FMA 2027. Onset was declared in June 2026 after a
  weak La Niña → neutral in early 2026; it started later than 1997/2015 but is
  "catching up" (NASA/JPL, Sentinel-6 Kelvin wave, June 2026). Papers so far:
  an OLAR paper on "extreme spring Pacific annular warming" elevating 2026/27,
  an arXiv climate-network forecast (Eastern-Pacific type, 77 %), plus GRL/
  general El Niño-teleconnection work. No peer-reviewed paper yet on the
  California coastal response.
- *California coast.* A marine heatwave (NOAA "NEP25A") has sat off central/
  southern California since **May 2025**, i.e. before the equator warmed;
  Blobtracker (19 Aug 2026) says the coastal warming is *not yet* directly El
  Niño-driven, and expects a south-to-north El Niño warming through spring/summer
  2027. Scripps Pier logged 42 record-high SST days by mid-Aug 2026; Rudnick is
  quoted that glider temperatures on the Point Conception and Monterey Bay lines
  are the highest in 20 years. A coastal Kelvin wave (+9–14 in. sea level in
  Central America, late Aug) is expected at San Francisco in early/mid-October;
  Merrifield expects record California sea levels. Our own cugn-climatology
  exploration (July 2026) already found the 2025-26 anomaly the largest surface
  anomaly in the CUGN record (+3.3 °C, Lines 80/90).
- *CUGN sources for Line 66.7 (current as of today):*

  | Source | Coverage | Notes |
  |---|---|---|
  | Local `$OS_SPRAY/CUGN/CUGN_line_66.nc` (this Linux box) | 2007-04 → **2023-03** | too old; file dated Jan 2025 |
  | 2026 beta climatology NetCDFs (`lt_`/`st_*_66.nc`) | → 2026-07-10 | **not on this machine** (were on the laptop) |
  | SprayData ERDDAP `binnedCUGN66` (delayed mode, 10-m bins) | 2007-04 → **2026-06-09** | T, S, chl, DO, backscatter, u/v |
  | SprayData ERDDAP `socal_index_10day_v1` / `_monthly_v1` (SCTI) | → 2026-06 | Line 90 index, ONI analog |
  | Glider DAC real-time `sp025-20260611T1755` | 2026-06-11 → **2026-09-03 (active)** | Line 66.7, has doxy + QARTOD flags |
  | Glider DAC `sp055-20260225T2146`, `sp025-20251118T1703` | 2025-11 → 2026-06 | previous Line 66.7 missions |

  A status script for this table is in `papers/ElNino/check_data_sources.py`.
  The spraydata El Niño page (updated 25 Jun 2026) defines its anomaly as the
  upper 100 m / inshore 200 km temperature departure from the seasonal cycle;
  the public climatology page is the 2023 (three-line) version.

**Questions**

1. **Which Line 66.7 data should drive the report, and where do I store it?**
   The local file ends in 2023 and the 2026 beta files are on the laptop. My
   recommendation: use the SprayData ERDDAP `binnedCUGN66` dataset for the
   delayed-mode record through June 2026 and the Glider DAC real-time mission
   (`sp025-20260611T1755`, and its successors) for the last ~3 months, stored
   under `$OS_SPRAY/CUGN/ElNino/`. Alternatives: copy the 2026 beta NetCDFs from
   the laptop, or re-download them. Your earlier rule was "no new data without
   asking", so I am asking.
>A.  I agree with your recommendation.  Put these data in `$OS_SPRAY/CUGN/Line_66/`

2. **Which anomaly definition?** Options: (a) the shipped 2026 beta interannual
   `anomaly` product (objectively mapped; long-term 2007–2014 or short-term
   2017–2025 baseline); (b) recompute mean + 3-harmonic annual cycle from the
   binned data with `cugn_climatology/climatology.py`, so the anomaly extends to
   the newest profiles. I recommend (b) with the **long-term 2007–2014 base
   years** (Rudnick et al. 2017 chose them to exclude the 2014–16 warm years),
   cross-checked against (a) where they overlap.
>A. Ok, let's do (b).  If you generate new code that will be useful for other project, put that in the `cugn/cugn/` folder in appropriate modules and/or subfolders.

3. **Headline index for Line 66.7?** Should I mirror the Southern California
   Temperature Index (upper 100 m, inshore 200 km, 3-month running mean) as a
   "Line 66.7 Temperature Index" plotted against the ONI, as
   `regenerate_figure.plot_anomaly_timeseries_oni` already does? Or a different
   depth/distance window for the Monterey line?
>A. Yes, mirror that.

4. **Which variables beyond temperature?** Candidates: salinity, depth of the
   26.0 kg m⁻³ isopycnal (the downwelling/Kelvin-wave diagnostic used in Rudnick
   2017), depth-averaged and geostrophic alongshore velocity (poleward flow),
   dissolved oxygen, chlorophyll, backscatter, mixed-layer depth. My default:
   T, isopycnal depth, velocity, DO, chlorophyll; skip backscatter.
>A. Use T and DO

5. **Framing and comparisons.** Should the report (a) treat the 2025-26 marine
   heatwave as the prelude, i.e. the story is "MHW hands off to El Niño", and
   (b) compare against the 2009-10 and 2015-16 El Niños and the 2014-15 Blob as
   they appeared on Line 66.7? 1997-98 predates the gliders. Should Lines 80/90
   appear for context (south-to-north propagation), or strictly Line 66.7?
>A. Yes, I like that.

6. **Ancillary datasets.** Which, if any, may I download: ONI (already handled by
   `load_oni`), the SCTI from the SprayData ERDDAP, the CUTI/BEUTI upwelling
   indices (`$OS_CCS` per CLAUDE.md), Monterey tide-gauge sea level (Kelvin wave
   arrival), satellite SST? I'd start with ONI + SCTI + upwelling index.
>A. You can download all of these, including the SST data.

7. **Report form, audience, home.** Markdown with embedded PNGs as in
   `cugn-climatology/context/reports/`? Where should it live:
   `cugn/papers/ElNino/`, `cugn-climatology/context/reports/`, or elsewhere?
   Audience: colleagues (Dan Rudnick's group) or a public/blog readership? For
   monthly updates I propose one living document with a dated "Update log"
   section and dated figure filenames, rather than a new report each month.
>A. Yes, Markdown.  We will put it in `Oceanography/python/cugn/reports/El_Nino_2026` and name this first one `El_Nino_2026_09_03.md`.  The audience for this report will be Dan Rudnick and like scientists.  We will then generate a one pager for the public.

8. **Code home and reuse.** The reusable helpers live in `cugn-climatology`
   (`climatology.py`, `regenerate_figure.py`, `analysis/iav_*`). `cugn-climatology`
   depends on `cugn`, so importing it from `cugn` would be circular. Options:
   put the El Niño code in `cugn-climatology` (my preference), or in
   `cugn/papers/ElNino/` with light copies of the helpers.
>A. The `cugn-climatology` repository was a sandbox.  I am going to ignore it eventually.  So copy over whatever you need into this `cugn` repository.

9. **Literature and news.** Should the report cite news pieces (Times of San
   Diego, KQED, SF Chronicle, Spokesman) or only NOAA/IRI/peer-reviewed sources?
   Note several Scripps and NOAA news pages return 403 to my fetches; if you
   want them used, please save the text/PDF somewhere I can read. Should I keep a
   running bibliography file (e.g. `papers/ElNino/sources.md`)?
>A.  It can cite both.  And, yes, keep a running bibliography file in `reports/El_Nino_2026/sources.md`.

10. **Machine.** Should I assume this Linux box is the working machine from now
    on (`$OS_SPRAY = /home/xavier/Oceanography/data/Spray/`) and stop relying on
    the laptop's copies?
>A. Yes, although I will likely copy over the files onto my laptop too.

---

**Round 2 (2026-09-03).** Your answers are clear; these are the concrete
choices I would otherwise make silently. Each has my recommended default so a
bare "OK" suffices. New facts that drove them: the SprayData `binnedCUGN66`
dataset holds 62,062 profiles (10,311 since the local file ends, missions
54–65); the Glider DAC real-time feed is **full-resolution Level-2** data
(~2.8 m spacing, QARTOD flags), not 10-m bins, so it must be binned by us;
`$OS_CCS/Upwelling/` already holds CUTI/BEUTI daily files (dated Dec 2024);
the `data/SST/` tree holds MODIS/VIIRS/H09 L2 extractions for other projects,
nothing gridded; NOAA CO-OPS serves the Monterey tide gauge (9413450) by API;
OISST v2.1 is current to 2026-08-31 and MUR to 2026-09-02; the `ocean14`
environment on this box lacks `gsw`, `cmocean`, `seawater`, `erddapy`.

11. **Layout of `$OS_SPRAY/CUGN/Line_66/`.** Proposed:

    | Path | Content | Refresh |
    |---|---|---|
    | `Line_66/binnedCUGN66.nc` | full ERDDAP dump (T, S, chl, DO, backscatter, u/v, depth-mean u/v), ~200 MB | re-download monthly, overwrite |
    | `Line_66/binnedCUGN66_<date>.nc` | dated snapshot of the above at each report | keep |
    | `Line_66/DAC/sp0xx-<start>.nc` | one file per real-time Line 66.7 mission, raw Level-2 | re-download the active one monthly |
    | `Line_66/DAC/binned_sp0xx-<start>.nc` | our 10-m binned version of the same | rebuilt from the raw |

    Full re-download rather than appending to the 2023 local file, so the record
    is always Dan's latest QC. OK? Keep dated snapshots or overwrite only?
>A. Yes, this is a good plan.

12. **Binning the real-time DAC data to match `binnedCUGN66`.** Proposed: keep
    QARTOD flags 1 (good) and 2 (not evaluated), drop 3/4/9; average into the
    same 10-m bins (10–500 m); project to along-line distance with
    `cugn.utils.line_endpoints` / `calc_dist_offset`; use the DAC data only for
    times after the ERDDAP dataset's last timestamp (so a mission is never
    counted twice once Dan's delayed-mode version appears). Accept flag 2?
>A. Ok, keep flag 2 for now.  But code it so we can turn that off.

13. **Ancillary data homes.** Proposed, matching the `$OS_CCS` convention:

    | Dataset | Source | Path |
    |---|---|---|
    | ONI / Niño-3.4 | NOAA CPC `detrend.nino34.ascii.txt` (Dan's `downloadoni.m` URL) | `$OS_CCS/Indices/ONI/` |
    | SCTI (10-day + monthly) | SprayData ERDDAP `socal_index_*_v1` | `$OS_CCS/Indices/SCTI/` |
    | CUTI / BEUTI daily | mjacox.com (refresh the Dec-2024 files) | `$OS_CCS/Upwelling/` (overwrite in place, keep `_2024` copies) |
    | Monterey tide gauge 9413450 (+ San Francisco 9414290 for the long record) | NOAA CO-OPS API, hourly + monthly MSL | `$OS_CCS/SeaLevel/` |
    | Gridded SST | see Q14 | `$OS_CCS/SST/` or `data/SST/<product>/` |

    Alternative for ONI: `$OS_SPRAY/CUGN/Climatology/ONI/` as cugn-climatology
    did. I prefer `$OS_CCS/Indices/` because none of these are glider data.
>A. Agreed, use $OS_CCS

14. **Which SST product and region?** Recommend **NOAA OISST v2.1** (daily,
    0.25°, 1981–present, so a 1991–2020 climatology and Hobday-style marine
    heatwave metrics are possible), subset to 25–50 °N, 135–115 °W (~1 MB/day,
    ~15 GB for the full record; or 2000–present only, ~10 GB). MUR (0.01°) is
    prettier for Monterey Bay but 2002+ and far larger. Store under
    `data/SST/OISST/` to match the existing per-product folders, or under
    `$OS_CCS/SST/`? And full record or 2000+?
>A. Yes, the NOAA OISST v2.1 is good.  Store it in `data/SST/OISST/`. 

15. **Sea-level anomaly method.** Monthly mean MSL minus the 1991–2020 monthly
    climatology, with the linear trend removed, plotted alongside the ONI for the
    Kelvin-wave arrival. Also show hourly-derived daily means for Oct–Nov 2026
    when the wave is due. OK?
>A. Ok.

16. **Anomaly recipe for (b), concretely.** Following Rudnick et al. (2017) and
    the `context_cugn_2026beta.md` parameters: project profiles to along-line
    distance; fit constant + 3 annual harmonics (period 365.25 d) per 10-m depth
    × 5-km bin with a 15-km Gaussian cross-shore weight; base years
    **2008–2013 for Line 66.7** (the paper's choice; the shipped `lt` files say
    2007–2014); anomaly = profile − fit; average anomalies into 10-day × 5-km
    bins **without** the objective-mapping step (I'd rather show the data than
    a 30-km/60-day smoothed map, and OI is a bigger port). Then the Line 66.7
    Temperature Index = upper 100 m, inshore 200 km, 3-month running mean, on
    the same axes as the ONI (and the SCTI for Line 90). OK with 2008–2013 and
    with skipping OI?
>A. Yes, ok with 2008–2013 and skipping OI.

17. **Dissolved oxygen baseline.** DO on the CUGN gliders is reliable only from
    ~2017, so its annual cycle cannot use 2008–2013. Proposal: fit the DO annual
    cycle over **2017–2024** (excluding the 2025–26 warm event) and state that
    the baseline differs from temperature's. Also show DO on isopycnals
    (σθ 25–27) as well as depth, since on Line 90 the depth-space DO signal was
    mostly isopycnal heave. OK?
>A. Yes, that is ok.

18. **Lines 80 and 90 for context.** Your Q5 answer implies the south-to-north
    story. That means also downloading `binnedCUGN80` / `binnedCUGN90` (each a
    similar ~200 MB) into `$OS_SPRAY/CUGN/Line_80/`, `Line_90/`, and computing
    their indices the same way. Do it now, or Line 66.7 only for the first
    report and add the others later?
>A. No, stick with Line 66.7 for now.  We may add the others later.

19. **Code homes inside `cugn/cugn/`.** Proposed:

    | Module | Content (ported from cugn-climatology where noted) |
    |---|---|
    | `cugn/erddap.py` | download helpers for SprayData ERDDAP, Glider DAC, DAC binning |
    | `cugn/annualcycle.py` (existing) | add `fit_harmonics()` / `evaluate_fit()` next to the existing evaluator of Dan's `.mat` fits |
    | `cugn/climatology.py` | mean + annual cycle + anomaly decomposition (`compute_climatology`, `trim_padding`) |
    | `cugn/indices.py` | ONI, SCTI, CUTI/BEUTI, tide gauge, Line temperature index, 3-month running mean |
    | `cugn/plotting.py` (existing) | add `plot_xz_section`, `plot_xtime_hovmoller`, `plot_anomaly_timeseries_oni`, `load_topography` |
    | `reports/El_Nino_2026/scripts/` | report-specific figure drivers, one PNG per figure into `reports/El_Nino_2026/figs/` |

    And move `papers/ElNino/check_data_sources.py` into
    `reports/El_Nino_2026/scripts/`. The two ESPER PNGs already in
    `reports/El_Nino_2026/` — are they part of this report or strays?
>A. Right, move the files from `papers/ElNino/` to `reports/El_Nino_2026/`.  I do not see any figures in `reports/El_Nino_2026/`.  

20. **Environment.** `gsw`, `cmocean`, `seawater`, and `erddapy` are absent from
    `ocean14` on this box (`process.py` itself imports `gsw`). May I
    `conda install -c conda-forge gsw cmocean` there (erddapy not needed; plain
    URLs suffice; `seawater` only if you want EOS-80 parity with Dan's MATLAB)?
>A.  Yes, add those and update the requirements.txt file for `cugn`.

21. **Monthly update mechanics.** Your naming (`El_Nino_2026_09_03.md`) implies
    a new file per update. Proposal: each month's file is a complete standalone
    report (copy the previous, refresh every figure and number, add a "What
    changed since last month" section at the top), figures written to
    `reports/El_Nino_2026/figs/<YYYY_MM_DD>/`, and one `make_report_figs.py`
    driver that takes the report date so the whole set regenerates in one run.
    OK, or a short delta document each month instead?
>A. Ok, I like the idea of a monthly report.  Modify the naming accordingly.

22. **Bibliography format.** `reports/El_Nino_2026/sources.md` as a Markdown
    list grouped by type (agency products, papers, news), each with URL, date
    accessed, and a one-line note on what it supports. Add a BibTeX file too?
>A.  Yes, and provide as DOI too if available.

---

**Round 3 (2026-09-03), final before building.** Two corrections first. The
two ESPER PNGs I mentioned in Q19 are in `nb/`, not `reports/El_Nino_2026/`;
that folder is empty, as you said. And `cugn/annualcycle.py`, `io.py`, and
`process.py` call `cugn.utils.calc_dist_offset`, which does not exist in
`cugn`; the function lives in `profiler/profiler/utils/offsets.py`, and
`profiler` is not importable in `ocean14` on this box (see Q31). Everything
below is about the report itself and the last build choices; after this I
proceed without further check-ins unless you say otherwise in Q34.

23. **Monthly naming.** `reports/El_Nino_2026/El_Nino_2026_09.md` for this
    first report, `El_Nino_2026_10.md` next month, etc., with figures in
    `reports/El_Nino_2026/figs/2026_09/`. Each report states its data cutoff
    (delayed-mode through 2026-06-09, real-time through the download date).
    The folder name `El_Nino_2026` stays through the 2027 months. OK?
>A. Ok

24. **Report outline.** Proposed sections for the ~10-page scientist-facing
    report; tell me what to add, drop, or reorder:
    1. Summary (half page: where the event stands, what Line 66.7 shows).
    2. ENSO state and forecast (CPC/IRI status, Niño indices, Kelvin wave).
    3. Line 66.7 sampling in the event (missions, profile counts, gaps).
    4. Temperature: the Line 66.7 Temperature Index vs ONI and SCTI; the
       upper-100 m distance–time anomaly; the inshore depth–time anomaly;
       the latest section vs the seasonal climatology.
    5. Dissolved oxygen: same three views on depth and on isopycnals.
    6. The 2025–26 marine heatwave as prelude (glider + OISST view).
    7. Comparison with 2009–10, 2014–15 Blob, 2015–16 on Line 66.7.
    8. Regional context: OISST anomaly map, CUTI/BEUTI, coastal sea level.
    9. Outlook: what to watch for next month.
    10. Methods and data provenance (recipe, baselines, versions, download dates).
    11. Update log (what changed since the previous report).
>A. Looks great, thanks.

25. **Figure list (first cut, ~11 figures).** Ordered by priority; say which
    to cut or add. (1) Line 66.7 Temperature Index + ONI + SCTI, 2007–present;
    (2) upper-100 m T-anomaly Hovmöller (distance × time), 2007–present;
    (3) inshore 0–200 km T-anomaly Hovmöller (depth × time), 2007–present;
    (4) zoom of (2)+(3) on 2025–2026; (5) latest real-time section: T and
    T-anomaly with σθ contours; (6) DO index and DO anomaly Hovmöllers on depth
    and σθ; (7) event composite: Line 66.7 index for 2009–10, 2014–16, 2025–26
    aligned by calendar month; (8) OISST SST-anomaly map, latest week, with
    the CUGN lines drawn; (9) OISST time series in a Monterey Bay box with
    marine-heatwave shading; (10) CUTI/BEUTI anomaly at 36–37 °N; (11)
    Monterey (and San Francisco) sea-level anomaly vs ONI.
>A. Also looks great.

26. **What "the latest section" means.** The most recent complete transect
    (a full inshore↔offshore leg) of the active DAC mission, binned to 10 m ×
    5 km, shown as absolute T and as anomaly from the seasonal climatology
    evaluated at the transect's mid-date. Fall back to the most recent 10-day
    bin if the leg is incomplete. OK?
>A. Yes, that is ok.

27. **Event comparison method.** Define events by ONI: 2009–10 El Niño
    (Jul 2009–Mar 2010), 2014–15 Blob (non-ENSO warm period, Jan 2014–Jun 2015),
    2015–16 El Niño (Mar 2015–May 2016), 2025–26 MHW → 2026–27 El Niño
    (May 2025–present). Overlay the Line 66.7 index by calendar month
    (Jul of year 0 → Jun of year +1) and tabulate peak index, month of peak,
    and depth penetration (depth where anomaly falls below +0.5 °C) per event.
    OK?
>A. Ok

28. **Marine-heatwave metric.** For OISST: Hobday et al. (2016) definition
    (1991–2020 daily climatology, 90th percentile, 5-day minimum) in a Monterey
    Bay box (36.3–37.0 °N, 122.6–121.8 °W) and along the Line 66.7 track. For
    the glider anomaly: no Hobday, just anomaly thresholds (+1, +2 °C) and the
    rank of the current value in the 2007–present record. OK?
>A. Ok

29. **Which ONI.** Dan's `downloadoni.m` uses CPC's detrended Niño-3.4
    (`detrend.nino34.ascii.txt`). CPC's status statements refer to the official
    ONI (`oni.ascii.txt`, ERSSTv5 with rolling 30-year base periods). Proposal:
    plot the **official ONI** as the headline and keep the detrended file for
    parity with the spraydata figures. Both go in `$OS_CCS/Indices/ONI/`. OK?
>A. Ok.

30. **OISST download strategy.** The CoastWatch ERDDAP aggregate allows
    server-side subsetting (25–50 °N, 135–115 °W → tens of KB per day) but lags
    (last day 2026-08-18); NCEI's daily global files are current (through
    2026-08-31, 1.6 MB each) but NCEI's subset service is returning 503 today.
    Proposal: ERDDAP subset for 1981-09 → the aggregate's last day, NCEI daily
    global files for the remaining days (subset locally, then delete the
    globals), saved as one regional NetCDF per year in `data/SST/OISST/`
    (~1 GB total for the full record). Full record from 1981, or 1991+?
>A. That is a good plan. And, let's do 1991+

31. **The `calc_dist_offset` dependency.** Options: (a) `pip install -e
    ../profiler` into `ocean14` and change the three call sites to import from
    `profiler.utils.offsets` (touches existing `io.py`/`process.py`); (b) copy
    the function into `cugn/utils.py`, leaving the call sites as written. I
    recommend (b) — it makes the existing calls correct with no other change —
    and will also install `profiler` so tests can run. OK?
>A.  I have just installed `profiler` into `ocean14`.  Use it.

32. **Equation of state.** Use `gsw` (TEOS-10) for σθ and isopycnal DO, and
    skip `seawater`/EOS-80, noting the ~0.01 kg m⁻³ offset from Dan's MATLAB in
    the Methods section. OK?
>A. Yes, use `gsw` for σθ and isopycnal DO.  Skip `seawater`/EOS-80.

33. **Tests.** Add `cugn/tests/test_annualcycle.py`, `test_climatology.py`,
    `test_indices.py` on synthetic data (no network, no data files) so they
    pass anywhere; network-dependent download tests marked `skip` unless an
    env flag is set. OK?
>A. Ok.

34. **Execution.** Once you answer, I plan to run the whole build in one go:
    installs, all downloads (~200 MB glider + ~1 GB OISST + small indices),
    porting/writing the modules and tests, the figure driver, `sources.md` +
    `sources.bib`, and the report — using parallel subagents for the
    independent pieces and asking nothing further unless a download fails or a
    result looks wrong. Expect several hours of work. OK to proceed that way,
    and any time budget?
>A. Yes, proceed that way.  Take as long as you need.  I will be away from this computer for the rest of the day.

35. **Public one-pager later.** Should the report figures already be built in
    a style reusable for the public version (large fonts, one message per
    panel, no jargon in titles), or is a separate simplified set fine later?
>A. We can make a separate simplified set for the public one-pager later.

## Logging

Append an entry to the **Logs** section of this file using the format:

```
### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>
```

## Logs

### 2026-09-03 (Prompt 1: gathered context on the 2026-27 El Niño; asked questions; no report yet)

**Context read.** Both cugn-climatology context docs
(`context_cugn_2026beta.md`: the mean + annual-cycle + OI-anomaly pipeline,
parameters, masks, Rudnick 2017 methods; `initial_exploration.md`: 80-file 2026
beta inventory, the SCTI-like all-lines ONI figure, and the July-2026 finding
that the 2025-26 warm event is the largest surface anomaly in the record), the
two existing reports in `context/reports/`, and the reusable toolkit
(`climatology.py`, `regenerate_figure.py` incl. `load_oni` /
`plot_anomaly_timeseries_oni`, `analysis/iav_*`).

**Websites.** spraydata El Niño page (SCTI vs ONI; anomaly = upper 100 m,
inshore 200 km; "El Niño conditions present, forecast to strengthen", updated
25 Jun 2026); spraydata climatology page (still the 2023 three-line version;
points to the new DO/56/along-shore climatologies); CUGN project page (five
lines; Line 66 NetCDF + ERDDAP + DAC real-time); IOOS Glider DAC overview.
Then dug into the ERDDAPs directly: SprayData `binnedCUGN66` runs to
2026-06-09 and `socal_index_10day_v1` to 2026-06-30; the DAC lists three recent
Line 66.7 missions, with `sp025-20260611T1755` active through today
(2026-09-03) and carrying doxy + QARTOD flags.

**Web search.** NOAA CPC discussion (13 Aug: Advisory; Niño-3.4 +1.4, Niño-1+2
+2.9, >90 % very strong, 69 % chance of a record OND ≥ +2.5), IRI plume (19 Aug:
25/26 models ≥ +2.0 through DJF, peak OND), NOAA onset release (June), NASA/JPL
Kelvin-wave story (June), Blobtracker (19 Aug: NEP25A off California since May
2025, coastal warming not yet El Niño-driven, S→N warming expected through
2027), NOAA CCIEA 2025-26 ecosystem report, news (Times of San Diego 25 Aug:
42 record-SST days at Scripps Pier; KQED 26 May: Monterey Bay MHW, seabird
die-offs; SF Chronicle/Gizmodo: ~1 ft El Niño sea-level rise, Merrifield
expects record CA sea levels; CBS8/Scripps: Rudnick says Pt Conception and
Monterey glider lines are warmest in 20 years). Papers: OLAR "Extreme Spring
Pacific Annular Warming Elevates the 2026/27 El Niño"; arXiv 2602.14773
climate-network forecast (EP-type 77 %); UH Mānoa GRL 15-month prediction;
Beniche 2026 GRL on teleconnections. Nothing peer-reviewed yet on the
California coastal response. Several scripps.ucsd.edu and noaa.gov news pages
returned 403 to my fetcher.

**Data reality check on this machine.** `$OS_SPRAY/CUGN/CUGN_line_66.nc` ends
2023-03-07 (file dated Jan 2025) and the 2026 beta climatology NetCDFs are not
present here (they lived on the laptop), so the report cannot be built from
local files alone. Wrote `papers/ElNino/check_data_sources.py` (ran clean in
`ocean14`) to print the local-file span, the SprayData ERDDAP CUGN/SCTI spans,
and the DAC Scripps deployments flagged as Line 66.7, for reuse at each monthly
update. Location is provisional pending Q8.

**Questions posted** in Q&A/Report (10): data source + storage; anomaly
definition/baseline; headline index for Line 66.7; variable set; framing
(MHW→El Niño, comparison events, other lines); ancillary downloads;
report form/audience/home/update scheme; code home (cugn vs cugn-climatology);
literature/news citation policy; working machine. Report not started, per the
prompt.

### 2026-09-03 (Prompt 2: read the answers; posted a second round of 12 questions; no report yet)

**Answers absorbed.** Data from SprayData ERDDAP + Glider DAC into
`$OS_SPRAY/CUGN/Line_66/`; recompute the anomaly ourselves (option b) with
reusable code going into `cugn/cugn/`; mirror the SCTI for Line 66.7; variables
T and DO; frame as MHW → El Niño with 2009-10 / 2015-16 / Blob comparisons and
S→N context; all ancillary data allowed incl. SST; Markdown report in
`reports/El_Nino_2026/El_Nino_2026_09_03.md` for Dan Rudnick-level readers, a
public one-pager later; copy what is needed out of the (to-be-abandoned)
cugn-climatology sandbox; cite agencies, papers, and news; running bibliography
in `reports/El_Nino_2026/sources.md`; this Linux box is the working machine.

**Facts gathered for round 2.** `binnedCUGN66` = 62,062 profiles, 10,311 since
the local file's 2023-03 end (missions 54–65); variables T, S, chl, DO,
backscatter, u/v, depth-mean u/v. The DAC mission `sp025-20260611T1755` is
Level-2 real-time, full resolution (~2.8 m), with QARTOD flags, 757 profiles
to date — so it needs our own 10-m binning. `$OS_CCS/Upwelling/` already has
CUTI/BEUTI daily NetCDFs from Dec 2024; `data/SST/` has only L2 swath
extractions (MODIS/VIIRS/H09); NOAA CO-OPS API serves Monterey 9413450 monthly
MSL; OISST v2.1 current to 2026-08-31, MUR to 2026-09-02; mjacox.com serves
CUTI/BEUTI daily+monthly csv/nc. The `ocean14` env here lacks gsw, cmocean,
seawater, erddapy. `cugn/annualcycle.py` already evaluates Dan's MATLAB
harmonic fits (`anncycXX.mat`; only `anncyc90.mat` is on disk) — the new
harmonic *fit* belongs beside it. `reports/El_Nino_2026/` exists and already
holds two ESPER PNGs. cugn-climatology helpers to port total ~1,000 lines
(`climatology.py`, `regenerate_figure.py`, `iav_*`).

**Questions posted (11–22):** Line_66 directory layout + snapshot policy; DAC
binning/QARTOD policy and no-double-counting rule; ancillary homes under
`$OS_CCS/{Indices,Upwelling,SeaLevel,SST}`; SST product (OISST recommended),
region, span, and folder; sea-level anomaly method; the concrete anomaly recipe
(2008–2013 base years, 15-km Gaussian, 3 harmonics, no OI); DO baseline
2017–2024 + isopycnal view; whether to pull Lines 80/90 now; module map inside
`cugn/cugn/` and moving `check_data_sources.py`; permission to conda-install
gsw + cmocean; monthly update mechanics (standalone dated reports, dated fig
folders, one driver script); bibliography format. Nothing downloaded, no code
moved, report not started.

### 2026-09-03 (Prompt 3: read round-2 answers; posted the final round of 13 questions; no report yet)

**Answers absorbed (11–22).** Line_66 layout approved (full ERDDAP dump +
dated snapshots + per-mission DAC raw and binned files); keep QARTOD flag 2
but make it switchable; ancillary data under `$OS_CCS/{Indices,Upwelling,
SeaLevel}`; OISST v2.1 in `data/SST/OISST/`; sea-level anomaly method OK;
anomaly recipe OK (2008–2013 base years, 15-km Gaussian, 3 harmonics, no OI);
DO baseline 2017–2024 with an isopycnal view; Line 66.7 only for now; code
map inside `cugn/cugn/` approved, move `papers/ElNino/` into
`reports/El_Nino_2026/`; conda-install gsw + cmocean and update
`requirements.txt`; monthly standalone reports; `sources.md` + BibTeX with
DOIs.

**Corrections found while preparing round 3.** (1) The two ESPER PNGs are in
`nb/`, not `reports/El_Nino_2026/` (my misread of an `ls`); the reports folder
is empty. (2) `cugn.utils.calc_dist_offset` is called by `annualcycle.py`,
`io.py`, and `process.py` but is not defined in `cugn`; it lives in
`profiler/profiler/utils/offsets.py`, and `profiler` does not import in
`ocean14` on this box. (3) OISST access: the CoastWatch ERDDAP aggregate
subsets server-side but lags (last day 2026-08-18); NCEI daily global files
are current (1.6 MB/day) but NCEI's THREDDS subset service returned 503
today. `requirements.txt` already lists gsw (not installed here); tests in
`cugn/tests/` depend on laptop data paths.

**Questions posted (23–35):** monthly file naming (`El_Nino_2026_09.md`,
`figs/2026_09/`); an 11-section report outline; an 11-figure list by
priority; definition of "latest section"; event windows and composite method;
marine-heatwave metric (Hobday on OISST, thresholds on glider anomaly);
official ONI vs Dan's detrended Niño-3.4; hybrid OISST download plan and
record start; how to fix the `calc_dist_offset` dependency (copy into
`cugn/utils.py` recommended); TEOS-10 only; synthetic-data tests; permission to
run the full build unattended with subagents; whether to style figures for
the later public one-pager. Nothing downloaded or installed, no code moved,
report not started.

### 2026-09-03 (Prompt 4: built the pipeline, downloaded all data, made 11 figures, wrote the September report)

**Environment.** `conda install gsw cmocean` into `ocean14` (gsw 3.6.23, cmocean
3.0.3); `pip install -e . --no-deps` so `cugn` imports from any directory (it
had only worked from the repo root); `requirements.txt` and `setup.py` updated
(cmocean, netcdf4, profiler note). `profiler` had been installed by the user.

**Data downloaded (all 2026-09-03).** `$OS_SPRAY/CUGN/Line_66/binnedCUGN66.nc`
(447 MB, 62,062 profiles 2007-04-19 → 2026-06-09; dated snapshot alongside) and
`Line_66/DAC/sp025-20260611T1755.nc` (49 MB, 757 real-time profiles → 2026-09-03)
plus our 10-m binned version. `$OS_CCS/Indices/ONI/` (official ONI + detrended
Niño-3.4), `$OS_CCS/Indices/SCTI/` (10-day + monthly), `$OS_CCS/Upwelling/`
(CUTI/BEUTI refreshed to 2026-08-30; Dec-2024 files kept as `*_2024.nc`),
`$OS_CCS/SeaLevel/` (Monterey 9413450 + San Francisco 9414290 monthly MSL
1991→2026-07, hourly Jun→Sep 2026, station metadata), `data/SST/OISST/`
(regional 25–50 °N, 135–115 °W, one file per year 1991→2026 from the CoastWatch
ERDDAP aggregate topped up with NCEI daily files).

**Code (reusable, in `cugn/cugn/`).** `erddap.py` (SprayData + Glider-DAC
download, flat→(profile, depth) pivot with along-line distance via
`profiler.utils.offsets.calc_dist_offset`, QARTOD-flag-switchable 10-m binning
of real-time data, no-double-counting combine); `annualcycle.py` (+
`harmonic_design`, `fit_annual_cycle` = Gaussian-windowed WLS constant + 3
harmonics, `evaluate_annual_cycle`; also fixed its broken `calc_dist_offset`
import); `climatology.py` (profile anomalies, 10-day × 5-km bin grids without
OI, TEOS-10 σθ, isopycnal interpolation, transect finder, section binning);
`indices.py` (ONI, SCTI, CUTI/BEUTI, tide gauges, running mean, line
temperature index, event windows; by subagent); `oisst.py` (download/update,
load, box and track means, Hobday MHW, latest anomaly map; by subagent);
`plotting.py` (+ `plot_xz_section`, `plot_xtime_hovmoller`,
`plot_ztime_hovmoller`, `plot_index_with_oni`). Tests on synthetic data:
`cugn/tests/test_{annualcycle,climatology,erddap,indices,oisst}.py`.
Report drivers in `reports/El_Nino_2026/scripts/`: `build_products.py`
(products → `$OS_SPRAY/CUGN/Line_66/products/`), `make_report_figs.py --date`
(PNGs + `stats.json` → `figs/2026_09/`), `check_data_sources.py` (moved from
`papers/ElNino/`).

**Bugs met and fixed.** Profiles with NaT time / NaN position broke the
distance projection and made the record's max time NaT (dropped 34 profiles);
the DAC data lacked a `mission` coordinate for the concat; the isopycnal-depth
fit had no data before 2017 when derived from the DO interpolation (now from
T); the "latest complete transect" first picked the leg still under way (now a
leg must also end within 15 % of the line range of either end); the event
table's depth structure used a single 10-day bin at the peak, which could be a
coverage gap (now ±45 days).

**Results (Line 66.7 index = 10–100 m, 0–200 km, 3-month mean, 2008–2013
base).** Positive since Jul 2025; peak **+1.90 °C, Apr 2026** (2015–16 peak
+1.78, Blob +1.39, 2009–10 +1.06; record +2.20 in a non-ENSO Nov-2017
episode); latest **+1.32 °C** (window centred 24 Aug), higher than any past
event at the same calendar stage. Peak 10-m anomaly +3.9 °C (15 Jun 2026,
55 km). Unlike the Blob, the warmth reaches deep: +1.0 °C at 100 m and +0.5 at
200 m at the peak (2015–16: +0.8/+0.3); isopycnals inshore 18–31 m deeper than
normal in Jun–Sep 2026. Latest complete transect (offshore leg 24 Jul–10 Aug,
149 profiles): +1.1 °C over 10–100 m, +0.45 over 100–500 m, max +4.7 at 40 m,
thin near-normal upwelled surface layer inshore. Oxygen: ~0 on depth surfaces
in the upper 100 m but **−30 / −19 / −10 µmol kg⁻¹ on σθ 25.5 / 26.0 / 26.5**
(2017–2024 base). CUTI/BEUTI above normal since spring 2026; Monterey / SF
July sea-level anomaly +6.1 / +5.0 cm; no Kelvin-wave arrival yet. ONI MJJ
+1.39; SCTI +1.27 (30 Jun).

**Report.** `reports/El_Nino_2026/El_Nino_2026_09.md` (~4,300 words, 11
sections per Q24, 11 figures per Q25, Table 1 of events), with
`sources.md` (45 annotated entries) and `sources.bib` (49 entries, all DOIs
verified by the bibliography subagent). Nothing committed (user does git).

**OISST (subagent + figs 8–9).** Regional archive 1991→2026-09-01 in
`data/SST/OISST/` (the subagent was still back-filling a few 1993–1998 gap
days when the figures were made). 7-day anomaly ending 1 Sep: +0.9 °C region
mean and along the Line 66.7 track, +3–4 °C in the SoCal Bight, Monterey box
+0.5 °C. Hobday MHW in the Monterey box: 164 of 489 days since 1 May 2025 in
seven events, incl. a Strong event 16 Jun–7 Jul 2026 (peak +3.2 °C); not in
MHW on 1 Sep. Fig 8 first rendered as a bare colorbar because cartopy's
Natural-Earth features failed; redrawn on plain lon/lat axes with the OISST
land mask.
