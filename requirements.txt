# Runtime dependencies for cugn.
#
# Keep this list in sync with install_requires in setup.py. The core
# scientific stack below is imported directly by the cugn package
# (numpy, scipy, pandas, matplotlib, scikit-learn, xarray, gsw, tqdm, IPython).

# Core scientific stack
numpy
scipy
pandas
matplotlib
seaborn
scikit-learn
tqdm
IPython

# Oceanography / gridding
xarray
h5netcdf
cftime
gsw
gsw-xarray
xhistogram
dask

# I/O and plotting
pymatreader
bokeh
importlib-metadata

# Sibling package (not on PyPI): base profiler data classes and pair analysis.
# Install from the local checkout alongside cugn (see CLAUDE.md):
#   pip install -e ../profiler
