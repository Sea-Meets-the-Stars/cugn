# Working on the September 2026 El Nino reports

This prompt doc will guide the reports for September 2026.

## Context 

See the `cugn-climatology` repository located under `Oceanography/python`.
There are two files in `context/` that you have generated:

- `context/context_cugn_2026beta.md`
- `context/initial_exploration.md`

See the CUGN webpages at https://spraydata.ucsd.edu/`
In particular:

- `https://spraydata.ucsd.edu/products/el-nino/`
- `https://spraydata.ucsd.edu/products/cugn-climatology/`

Glider data from CUGN is actively placed at this website: `https://ioos.github.io/glider-dac/`

The first prompt doc used is `super_el_nino_prompts.md` in 
`Oceanography/python/cugn/claude_prompts`

### Newsletter

The newsletter is the file `El_Nino_public_Sep2026.md` in 
`Oceanography/python/cugn/reports/El_Nino_2026/newsletter`

## Code

Follow these guidelines:

- Use inline comments to explain the code.
- Use matplotlib
- Code in Python
- Place imports at the top of modules
- Reuse any code in this repository to help you
- Use functions and not classes
- Include a description of inputs/outputs in the doc string of all methods 
- Add "Generated by JXP and Claude" in the doc string of all methods you create

If you need to run Python code, please use the `ocean14` conda environment.

## Prompts

### Report

1. We are going to generate a report that describes the current state of the El Nino of 2026-2027 on Line 66.  
As the months go by, we will update it.  Begin by:

    - Examining all of the files and websites in context
    - Searching the web for stories and papers on the El Nino of 2026-2027
    - Ask me a set of questions in Q&A/Report to guide your efforts.  If you need me to 

Do not write the report yet.  Use Fable if you can. Log your work.

2. I have answered your questions in Q&A/Report.  Please read my responses and ask another round of questions before doing anything, e.g. discuss where to download the ancillary data onto this machine.  Do not write the report yet.  Use Fable if you can. Log your work.

3. I have answered your questions in Q&A/Report.  Please read my responses and ask another round of questions before doing anything.  I expect this will be the last round before proceeding with the report. Use Fable if you can. Log your work.

4. Ok, I have answered the last round of questions.  Please proceed with the report.  Use Fable if you can. Log your work.

### Newsletter

1. Read all the files in Context.  The first draft of the newsletter is complete.  I wish to improve the first figure, remove the 2nd figure and generate a new one to replace it.  Let's work on the first figure first.  Please make all of these changes:

   - Remove the 2015-2016 curve
   - Use red for the Now curve
   - Rename the Now curve as 2026-2027 (Now)
   - Rename last year as 2025-2026 (Last year)
   - Use yellow for last year
   - Use orange for the Blob year
   - Shade all of the region above 0 deg C in a pale pink
   - Shade all of the region below 0 deg C in a pale blue
   - Make the 0 deg line a bold, and dashed black line
   - Remove the Farenheit labeling on the right axis

If you have any questions, ask them in the Q&A section.  Use Fable if you can.  Log your work.

2. For the second figure, I would like to show the anoomolies of 
SST off the coast of California.  Similar to Figure 8 of the main 
report, but zoomed in more on Monterey Bay.  And for a public
audience.  Please generate one.  

If you have any questions, ask them in the Q&A section.  Use Fable if you can.  Log your work.

## Q&A

### Newsletter Fig. 1 (2026-09-12)

1. I kept the leadership briefing's version of this figure unchanged and restyled
   only the newsletter copy, so the two now differ. Is that what you want, or
   should the briefing adopt the same design next month?
2. Yellow on the pale pink background is legible but not strong. When the real
   figure is drawn, do you want a slightly deeper yellow (e.g. `#E8B800`) or a
   thin dark outline on that curve?
3. With the 2015-16 curve gone, the headline claim ("warmer than in any past
   event") is no longer shown by a curve on the plot. Keep the title as is, or
   soften it to something the three plotted curves support on their own?

## Logging

Append an entry to the **Logs** section of this file using the format:

```
### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>
```

## Logs

### 2026-09-12 (Newsletter Fig. 1 restyled; products not on this laptop)

**Code.** All ten requested changes to newsletter Figure 1 are implemented in a new
function `fig_past_events_public()` in
`reports/El_Nino_2026/scripts/make_newsletter.py`:

- 2015–16 El Niño curve removed (three curves remain);
- "Now" is red and relabelled **2026–2027 (Now)**;
- last year relabelled **2025–2026 (Last year)** and drawn in yellow (`gold`);
- the Blob stays orange (`darkorange`);
- everything above 0 °C shaded pale pink (`#FBDCE4`), everything below pale blue
  (`#D8E6F5`), with the y-limit dropped to −0.4 °C so the blue band is visible;
- the 0 °C line is a bold dashed black rule (lw 2.5, `--`);
- the Fahrenheit twin axis is gone.

I wrote a separate function rather than editing
`make_one_pager.fig_past_events()` so the leadership briefing that has already
gone out keeps its own figure; `figs_from_products()` now calls the public
version for Fig. 1 and still calls `make_one_pager.fig_index_oni()` for Fig. 2
(which prompt 2 will replace). The Fig. 1 caption and image alt text in
`build_markdown()` were rewritten to match the new colours ("three warm
periods"; red / yellow / orange; pink = warmer than normal, blue = cooler).

**Figure not regenerated.** `$OS_SPRAY/CUGN/Line_66/` is empty on this laptop —
no `products/line66_index.csv` — so the September figures in the repository were
originally recovered from the base64 images in the briefing HTML
(`make_newsletter.py --figs html`), and the new figure cannot be drawn without
rebuilding the products. I started `build_products.py`; the ERDDAP download of
`binnedCUGN66.nc` (streamed, no Content-Length) stalled at 155 MB after about two
minutes and made no further progress for ten, so I killed it. `_download()` uses
a 3000 s socket timeout, which is why a stalled transfer hangs for ~50 min rather
than failing fast. A dead `binnedCUGN66.nc.part` is left in
`$OS_SPRAY/CUGN/Line_66/`. JXP will fetch the data separately; after that,
`python reports/El_Nino_2026/scripts/make_newsletter.py --date 2026-09-03 --figs products`
regenerates Fig. 1, `README.md`, the dated Markdown copy and the WordPress HTML.

**Verified** the new code by rendering it with a synthetic index series in the
scratchpad (not committed, not glider data): colours, shading, dashed zero line
and the single left-hand °C axis all come out as specified.

**Learned.** The newsletter has two figure provenance paths (`products` vs
`html`), and on a laptop without `$OS_SPRAY` populated only the `html` path
works — which means any *new* figure design, like this one, forces a product
rebuild. Worth considering caching `line66_index.csv` in the repository (it is a
few tens of kB) so newsletter figures can be redrawn anywhere.

### 2026-09-13 (Newsletter Fig. 1 regenerated from the products)

The Line 66.7 products are now on this laptop
(`$OS_SPRAY/CUGN/Line_66/products/`, build date 2026-09-03, index last value
1.317 °C on 2026-08-24 — the same build the September report and its stats.json
came from), so Figure 1 was redrawn for real with `fig_past_events_public()`:

```
python reports/El_Nino_2026/scripts/make_newsletter.py --date 2026-09-03 --figs products
```

This rewrote `newsletter/figs/newsletter_fig1_past_events_Sep2026.png`,
`newsletter/README.md`, `newsletter/El_Nino_public_Sep2026.md` and
`newsletter/El_Nino_public_Sep2026_wordpress.html`.

**Two changes to the build were needed.**

1. `$OS_CCS/Indices/ONI/oni.ascii.txt` was missing, and `figs_from_products()`
   loaded the ONI before drawing either figure, so Fig. 1 could not be drawn.
   The ONI load now happens only when Fig. 2 is actually being drawn, and the
   file was fetched with `cugn.indices.download_oni('official')`.
2. The fresh CPC file runs to **JJA 2026 = +1.80 °C**, whereas the newsletter
   text quotes **+1.4 °C** from stats.json (the 2026-09-03 build). Redrawing
   Fig. 2 would therefore have put a curve in the page that disagrees with the
   sentence next to it, so `figs_from_products(fig1, fig2=None)` now leaves an
   existing Fig. 2 alone; `--redraw-fig2` forces it. Fig. 2 is unchanged on disk
   (md5 0715b123…), and prompt 2 replaces it anyway.

**The figure.** Red 2026–2027 curve starts at 1.41 °C and runs to 1.32 °C on
2026-08-24 (the 3-month running mean ends there); yellow 2025–2026 peaks at
1.90 °C in April; orange Blob peaks at 1.39 °C. The "warmer at this point in the
year than any past event" claim in the title and caption checks out against the
index: July starts are 2026 **1.41**, 2015 0.93, 2025 0.68, 2014 0.63, 2009 0.15
(`scratchpad/check_start.py`). Yellow on the pale pink background is legible at
lw 3.2; the blue band below zero is visible but empty, as expected for these
three warm periods.

**Learned.** Live feeds (ONI) and the frozen report products (stats.json) drift
apart between monthly builds; any figure that mixes them needs to be pinned to
the report date or the page contradicts itself. Worth doing the same check for
the sea-level and MHW numbers when the October update is built.
